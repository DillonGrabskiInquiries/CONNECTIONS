var names = ["james", "kyle", "jordan", "Jane Doe"]
var editProfile = document.getElementByID('#editButton')
var jane = document.getElementByID('#Jane')

function nameSwap() {
    for (i = 0; i < names.length; i++) {
        jane == math.floor(math.random(names.length.name[i]))
        console.log(jane)
    }
}